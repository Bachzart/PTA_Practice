#include <stdio.h>#include <string.h>#define MAXS 10void Shift( char s[] );void GetString( char s[] ); /* 实现细节在此不表 */int main(){    char s[MAXS];    GetString(s);    Shift(s);    printf("%s\n", s);	    return 0; }void GetString( char s[] ) {	gets(s);}void Shift( char s[] ) {	char str[MAXS], *p = s + 3;	int i = 0;	while(*p != '\0') str[i++] = *p++;	int j;	for(j = 0; j < 3; j++) str[i++] = s[j];	str[i] = '\0';	strcpy(s, str);}